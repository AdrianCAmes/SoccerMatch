@model IEnumerable<SoccerMatch_TF.Models.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ndepartamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ndepartamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Cdepartamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Cdepartamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Cdepartamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
