@model IEnumerable<Entity.Ciudad>

@{
    ViewBag.Title = "Ciudad";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CCiudad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NCiudad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CDepartamento.NDepartamento)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CCiudad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NCiudad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CDepartamento.NDepartamento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { id = item.CCiudad }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
